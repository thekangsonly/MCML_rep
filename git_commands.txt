
GIT setup

ls
cd

git init
git remote add origin <git link>
git remote  -v

// add everything at the current dir
git add . 

git commit -m "first commit"

git push origin master

git help


git config --global user.email "thekangsonly@gmail.com"
git config --global user.name "thekangsonly"

git add git_commands.txt
git commit -m "git commands"